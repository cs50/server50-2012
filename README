INSTALL

% sudo yum install rpm-build redhat-rpm-config
% echo '%_topdir /path/to/dev50/rpmbuild' > ~/.rpmmacros 

Source: http://wiki.centos.org/HowTos/SetupRpmBuildEnvironment


TODO

* /etc/sysconfig/iptables doesn't persist after install.
